version: 2.0
jobs:
  build:
    docker:
      - image: circleci/python:3.6.1

    working_directory: ~/repo

    steps:
      - checkout

      - run:
          name: install dependencies
          command:
            python setup.py install

      - run:
          name: run tests
          command: 
			      python ./Tests/tests.py
			      python ./Tests/tests_2.py
